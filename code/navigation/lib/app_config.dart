class AppConfig {
  // TODO PREP2 : Change cette valeur pour tester avec le Navigator (true) ou avec le Router (false)
  // Il faut redémarrer l'application au complet quand on change la valeur
  static const bool useNavigator = true;
}
